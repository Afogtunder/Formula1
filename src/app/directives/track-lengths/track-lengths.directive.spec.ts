import { TrackLengthsDirective } from './track-lengths.directive';

describe('TrackLengthsDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackLengthsDirective();
    expect(directive).toBeTruthy();
  });
});
