import { ChampionshipDirective } from './championship.directive';

describe('ChampionshipDirective', () => {
  it('should create an instance', () => {
    const directive = new ChampionshipDirective();
    expect(directive).toBeTruthy();
  });
});
