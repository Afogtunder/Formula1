import { BirthdateFormatPipe } from './birthdate-format.pipe';

describe('BirthdateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BirthdateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
